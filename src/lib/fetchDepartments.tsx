// import { Department } from '@/payload-types'

// export const fetchDepartments = async (): Promise<Omit<Department, 'createdAt' | 'updatedAt'>[]> => {
//     const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/api/departments?depth=2`)
//     const data = await res.json()
//     return data.docs
// }
