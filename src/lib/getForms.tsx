// lib/getForms.ts
// "use server";

// import { getPayload } from "payload";
// import config from "@/payload.config";

// export const getForms = async () => {
//     const payload = await getPayload({ config });

//     const forms = await payload.find({
//         collection: "forms",
//     });

//     return forms.docs.map(f => f.slug);
// };
